# Sourced as a shell script by scripts/build.sh under `set -e`.
# One KEY="value" per line. Every value double-quoted. No spaces around =.
# No command substitution. Keys are [A-Z0-9_]+ only.

BOOK_NAME="a_linear_algebra_primer_for_financial_engineering"
TITLE="A Linear Algebra Primer for Financial Engineering"
AUTHOR="Dan Stefanica"
EDITION="1"
SOURCE_PDF="pdfs/baruch_01_textbook.pdf"

# Real PDF page count: len(pymupdf.open(SOURCE_PDF))
PAGE_COUNT="341"

# Printed page 1 is PDF page 17, so PDF index = printed page + 16.
# Front matter is paginated i..xvi and aligns 1:1 with the PDF index.
PAGE_OFFSET="16"

# Never typeset. Recorded only so later phases skip it.
COPYRIGHT_PAGE="4"

# The book contains no figures at all: every graphic-like element is a framed
# pseudocode table captioned "Table N.M". Recorded as two-part because that is
# the numbering scheme the source uses for its float captions.
FIGURE_NUMBERING="two-part"

# All ranges below are 1-based PDF page indices, already offset-corrected.
CH01_PAGES="17-52"
CH02_PAGES="53-102"
CH03_PAGES="103-126"
CH04_PAGES="127-154"
CH05_PAGES="155-176"
CH06_PAGES="177-208"
CH07_PAGES="209-242"
CH08_PAGES="243-266"
CH09_PAGES="267-302"
CH10_PAGES="303-334"

# Bibliography 335-337, Index 338-341
BACKMATTER_PAGES="335-341"
