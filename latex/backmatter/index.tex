% index.tex — Index entries
%
% This file is a placeholder. The index is generated automatically by
% \printindex in main.tex (using the makeidx package).
%
% Actual index entries should be added as \index{} commands throughout
% the chapter source files. For example:
%   \index{heat equation}
%   \index{Fourier series!convergence}
%   \index{separation of variables}
%   \index{Bessel functions}
%   \index{Green's function}
%   \index{Sturm-Liouville problem}
%
% To build the index:
%   1. Run pdflatex main
%   2. Run makeindex main
%   3. Run pdflatex main (again)
%
% The index spans book pages 751–end (approximately 15 pages).
