% answers.tex — Answers to starred exercises
% Selected Answers to Starred Exercises (book pages 731–749)

\chapter*{Selected Answers to Starred Exercises}
\addcontentsline{toc}{chapter}{Selected Answers to Starred Exercises}

%% ══════════════════════════════════════════════════════════════
\section*{Chapter 1}
%% ══════════════════════════════════════════════════════════════

\textbf{1.2.8.}\quad $\displaystyle\int_0^L c\rho u\, A\, dx$

\medskip
\textbf{1.2.9.}\quad (e) $u(t) = u_0 \exp\!\left[-\left(\dfrac{2h}{c\rho r}\right)t\right]$

\medskip
\textbf{1.3.2.}\quad $K_0(x_{0-})\pd{u}{x}(x_{0-},t) = K_0(x_{0+})\pd{u}{x}(x_{0+},t)$

\medskip
\textbf{1.3.3.}\quad $Vc_l\rho_l\pd{u}{t}(L,t) = -K_0(L)\pd{u}{x}(L,t)A$, where $V$ is the volume of the bath

\medskip
\textbf{1.4.1.}\quad (a) $u(x) = \dfrac{Tx}{L}$

\quad (d) $u = T + \alpha x$

\quad (f) $u(x) = -\dfrac{x^4}{12} + \dfrac{L^3 x}{3} + T$

\quad (h) $u = T + \alpha(x+1)$

\medskip
\textbf{1.4.2.}\quad (a) $\dfrac{K_0 L^2}{2}$

\medskip
\textbf{1.4.7.}\quad (a) $\beta = 1 - L$

\medskip
\textbf{1.5.2.}\quad $\pd{u}{t} + \vec{v}\cdot\nabla u = k\nabla^2 u - u\nabla\cdot\vec{v}$ (often physically $\nabla\cdot\vec{v}=0$)

\medskip
\textbf{1.5.9.}\quad (a) $u = \dfrac{T_1\ln r_2/r + T_2\ln r/r_1}{\ln r_2/r_1}$

\medskip
\textbf{1.5.11.}\quad $\beta = \dfrac{b}{a}$

\medskip
\textbf{1.5.13.}\quad $u(r) = \dfrac{320}{3}\left(1 - \dfrac{1}{r}\right)$

%% ══════════════════════════════════════════════════════════════
\section*{Chapter 2}
%% ══════════════════════════════════════════════════════════════

\textbf{2.3.1.}\quad (a) $\od{h}{t} = -\lambda kh$ and $\dfrac{1}{r}\frac{d}{d r}\!\left(r\od{\phi}{r}\right) = -\lambda\phi$

\quad (c) $\odd{\phi}{x} = -\lambda\phi$ and $\odd{h}{y} = \lambda h$

\quad (e) $\dfrac{1}{kh}\od{h}{t} = \dfrac{1}{\phi}\odd{\phi}{x} = \lambda$

\quad (f) $\dfrac{1}{c^2 h}\odd{h}{t} = \dfrac{1}{\phi}\odd{\phi}{x} = -\lambda$

\medskip
\textbf{2.3.2.}\quad (b) $\lambda = (n\pi/L)^2$ with $L = 1$ so that $\lambda = n^2\pi^2$, $n = 1, 2, \ldots$

\quad (d) $\lambda = \left[\dfrac{(n-\frac{1}{2})\pi}{L}\right]^2$, $n = 1, 2, 3, \ldots$

\quad (f) $\lambda = \left(\dfrac{n\pi}{b-a}\right)^2$, $n = 1, 2, 3, \ldots$

\medskip
\textbf{2.3.3.}\quad (c) $u(x,t) = \sum_{n=1}^{\infty} A_n \sin\dfrac{n\pi x}{L} e^{-k(n\pi/L)^2 t}$, where
$A_n = \dfrac{2}{L}\int_0^L 2\cos\dfrac{3\pi x}{L}\sin\dfrac{n\pi x}{L}\,dx$

\medskip
\textbf{2.3.4.}\quad (a) $c\rho A \sum_{n=1}^{\infty} B_n e^{-k\left(\frac{n\pi}{L}\right)^2 t}\left(\dfrac{1-\cos n\pi}{n\pi}\right)$

\quad (c) Heat energy equals initial heat energy plus the time integral of the flow
in of the heat energy at the boundaries.

\medskip
\textbf{2.3.6.}\quad $0\ (n \neq m)$, $\frac{1}{2}\ (n = m \neq 0)$, $L\ (n = m = 0)$

\medskip
\textbf{2.3.8.}\quad (a) $u = 0$

\quad (b) $u = e^{-\alpha t}\sum_{n=1}^{\infty} b_n \sin\dfrac{n\pi x}{L} e^{-k(n\pi/L)^2 t}$

\medskip
\textbf{2.3.9.}\quad (a) If $\sqrt{-\alpha/k}\, L = n\pi$, then $u(x) = A\sin\dfrac{n\pi x}{L}$.

\quad (b) If $-\frac{\alpha}{k} = \left(\frac{\pi}{L}\right)^2$, $u(x,t) \to B\sin\dfrac{\pi x}{L}$, as $t \to \infty$. If $-\frac{\alpha}{k} < \left(\frac{\pi}{L}\right)^2$, then $u \to 0$ as $t \to \infty$. However, if $-\frac{\alpha}{k} > \left(\frac{\pi}{L}\right)^2$, $u \to \infty$ as $t \to \infty$.

\medskip
\textbf{2.3.10.}\quad (c) $\left[\int_0^L A(x)B(x)\,dx\right]^2 \leq \left(\int_0^L A^2\,dx\right)\left(\int_0^L B^2\,dx\right)$

\medskip
\textbf{2.4.1.}\quad $u = A_0 + \sum_{n=1}^{\infty} A_n \cos\dfrac{n\pi x}{L} e^{-k(n\pi x/L)^2 t}$

\quad (a) $A_0 = \frac{1}{2}$, $A_n = -\dfrac{2}{n\pi}\sin\dfrac{n\pi}{2}$ \quad ($n \neq 0$)

\quad (b) $A_0 = 6$, $A_3 = 4$, others $= 0$

\quad (c) $A_0 = -\frac{4}{\pi}$, $A_n = -\dfrac{4}{L}\int_0^L \sin\dfrac{\pi x}{L}\cos\dfrac{n\pi x}{L}\,dx$ can easily be evaluated using
trigonometric identities or tables of integrals.

\medskip
\textbf{2.4.2.}\quad $u(x,t) = \sum_{n=1}^{\infty} c_n \cos\dfrac{(n-\frac{1}{2})\pi x}{L} e^{-[(n-1/2)\pi/L]^2 kt}$, where
$c_n = \dfrac{2}{L}\int_0^L f(x)\cos\dfrac{(n-\frac{1}{2})\pi x}{L}\,dx$

\medskip
\textbf{2.4.3.}\quad $\lambda = n^2$, $\phi = \sin nx$ and $\cos nx$, $n = 0, 1, 2, 3\ldots$

\medskip
\textbf{2.5.1.}\quad (a) $u(x,y) = A_0 y + \sum_{n=1}^{\infty} A_n \cos\dfrac{n\pi x}{L}\sinh\dfrac{n\pi y}{L}$

\quad (c) $u(x,y) = \sum_{n=1}^{\infty} A_n \cosh\dfrac{n\pi x}{H}\sin\dfrac{n\pi y}{H}$, where
$A_n\cosh\dfrac{n\pi L}{H} = \dfrac{2}{H}\int_0^H g(y)\sin\dfrac{n\pi y}{H}\,dy$

\quad (e) $u(x,y) = \sum_{n=1}^{\infty} A_n h_n(y)\sin\dfrac{n\pi x}{L}$, $A_n h_n(H) = \dfrac{2}{L}\int_0^L f(x)\sin\dfrac{n\pi x}{L}\,dx$,
where $h_n(y) = \cosh\dfrac{n\pi y}{L} + \dfrac{L}{n\pi}\sinh\dfrac{n\pi y}{L}$% UNCLEAR: exact form of h_n, page 733

\medskip
\textbf{2.5.2.}\quad (a) $\int_0^L f(x)\,dx = 0$

\medskip
\textbf{2.5.3.}\quad $u(r,\theta) = \sum_{n=0}^{\infty} A_n r^{-n}\cos n\theta + \sum_{n=1}^{\infty} B_n r^{-n}\sin n\theta$

\quad (a) $A_0 = \ln 2$, $A_3 a^{-3} = 4$, other $A_n = 0$, $B_n = 0$

\quad (b) See (2.5.47) with $a^n$ replaced by $a^{-n}$.

\medskip
\textbf{2.5.4.}\quad $u(r,\theta) = \dfrac{a^2-r^2}{2\pi}\int_{-\pi}^{\pi}\dfrac{f(\bar\theta)\,d\bar\theta}{a^2+r^2-2ar\cos(\theta-\bar\theta)}$

\medskip
\textbf{2.5.5.}\quad (a) $u(r,\theta) = \sum_{n=1}^{\infty} A_n r^{2n-1}\cos(2n-1)\theta$

\quad (c) $u(r,\theta) = \sum_{n=1}^{\infty} A_n r^{2n}\sin 2n\theta$, $A_n = \dfrac{2}{a\pi}\int_0^{\pi/2} f(\theta)\sin 2n\theta\,d\theta$

\medskip
\textbf{2.5.6.}\quad (a) $u(r,\theta) = \sum_{n=1}^{\infty} B_n r^n \sin n\theta$

\medskip
\textbf{2.5.7.}\quad (b) $u(r,\theta) = \sum_{n=0}^{\infty} A_n r^{3n}\cos 3n\theta$

\medskip
\textbf{2.5.8.}\quad (a) $\phi_1(r) = \begin{cases} \ln(r/a) & n = 0 \\ \left(\frac{r}{a}\right)^n - \left(\frac{a}{r}\right)^n & n \neq 0 \end{cases}$\quad
$\phi_2(r) = \begin{cases} \ln(r/b) & n = 0 \\ \left(\frac{r}{b}\right)^n - \left(\frac{b}{r}\right)^n & n \neq 0 \end{cases}$

$u(r,\theta) = \sum_{n=0}^{\infty}\cos n\theta\,[A_n\phi_1(r)+B_n\phi_2(r)] + \sum_{n=1}^{\infty}\sin n\theta\,[C_n\phi_1(r)+D_n\phi_2(r)]$

$D_n\phi_2(a) = \dfrac{1}{\pi}\int_{-\pi}^{\pi}f(\theta)\sin n\theta\,d\theta$, etc.

\medskip
\textbf{2.5.9.}\quad (a) $u(r,\theta) = \sum_{n=1}^{\infty} A_n\left[\left(\frac{r}{a}\right)^{2n}-\left(\frac{a}{r}\right)^{2n}\right]\sin 2n\theta$, where $A_n\left[\left(\frac{b}{a}\right)^{2n}-\left(\frac{a}{b}\right)^{2n}\right] = \dfrac{4}{\pi}\int_0^{\pi/2}f(\theta)\sin 2n\theta\,d\theta$

\quad (b) $u(r,\theta) = \sum_{n=1}^{\infty} A_n\sinh\dfrac{n\pi\ln(r/a)}{\ln(b/a)}\sin\left[n\pi\dfrac{\ln(r/a)}{\ln(b/a)}\right]$, where $A_n\sinh\dfrac{n\pi}{2\ln(b/a)} = \dfrac{2}{\ln(b/a)}\int_a^b f(r)\sin\left[n\pi\dfrac{\ln(r/a)}{\ln(b/a)}\right]\,dr/r$% UNCLEAR: exact form, page 733

%% ══════════════════════════════════════════════════════════════
\section*{Chapter 3}
%% ══════════════════════════════════════════════════════════════

\textbf{3.2.1.}\quad % UNCLEAR: graphical answers showing periodic extensions, page 733--735

\medskip
\textbf{3.2.2.}\quad (a) $a_n = 0$, $b_n = \dfrac{2L}{n\pi}(-1)^{n+1}$

\quad (c) $b_1 = 1$, all others $= 0$

\quad (f) $a_0 = \frac{1}{2}$, other $a_n = 0$; $b_n = \dfrac{2}{n\pi}$ ($n$ odd), other $b_n = 0$

\medskip
\textbf{3.3.1.}\quad (d) % UNCLEAR: graphical answers showing Fourier sine/cosine series extensions, page 735

\medskip
\textbf{3.3.2.}\quad (d) repeat periodically, $b_n = \dfrac{2}{n\pi}\left(1 - \cos\dfrac{n\pi}{2}\right)$

\medskip
\textbf{3.3.10.}\quad
$f_e(x) = \frac{1}{2}[f(x)+f(-x)] = \frac{1}{2}\begin{cases} x^2 + e^x & x < 0 \\ x^2 + e^{-x} & x > 0 \end{cases}$

$f_o(x) = \frac{1}{2}[f(x)-f(-x)] = \frac{1}{2}\begin{cases} x^2 - e^x & x < 0 \\ e^{-x} - x^2 & x > 0 \end{cases}$

\medskip
\textbf{3.3.13.}\quad $b_n = \dfrac{2}{L}\int_0^L f(x)\sin\dfrac{n\pi x}{L}\,dx$, $b_n = 0$ for $n$ even, since $\sin n\pi x/L$ is odd around $L/2$ for $n$ even

\medskip
\textbf{3.4.1.}\quad (a) $\int_a^b u\odd{v}{x}\,dx = uv\Big|_a^{c-} + uv\Big|_{c+}^b - \int_a^b v\odd{u}{x}\,dx$% UNCLEAR: exact form of jump condition, page 736

\medskip
\textbf{3.4.3.}\quad (a) $b_n = \dfrac{2}{L}\sin\dfrac{n\pi a}{L}(\alpha-\beta) - \dfrac{n\pi}{L}a_n$

\medskip
\textbf{3.4.9.}\quad $\od{A_n}{t} + k\left(\dfrac{n\pi}{L}\right)^2 b_n = \dfrac{2}{L}\int_0^L q(x,t)\sin\dfrac{n\pi x}{L}\,dx$

\medskip
\textbf{3.4.12.}\quad $u = \sum_{n=0}^{\infty} A_n(t)\cos\dfrac{n\pi x}{L}$,

$n \neq 0,\,3$:\quad $A_n(t) = A_n(0)e^{-k(n\pi/L)^2 t}$

$A_0(t) = A_0(0) + 1 - e^{-t}$

$A_3(t) = A_3(0)e^{-k(3\pi/L)^2 t} + \dfrac{e^{2t} - e^{-k(3\pi/L)^2 t}}{k(\frac{3\pi}{L})^2 - 2}$% UNCLEAR: sign in denominator, page 736

\medskip
\textbf{3.5.1.}\quad (c) $\left(\frac{\pi}{L}\right)^3 = \frac{1}{4} + \sum_{n=1}^{\infty}\left\{\dfrac{6(-1)^n}{(n\pi)^2} + 12\dfrac{1-(-1)^n}{(n\pi)^4}\right\}\cos\dfrac{n\pi x}{L}$\quad $0 \leq x \leq L$

\medskip
\textbf{3.5.4.}\quad $b_n = \dfrac{2n\pi}{L^2+n^2\pi^2}\left[1 - (-1)^n\cosh L\right]$

\medskip
\textbf{3.5.7.}\quad $\pi^3/32$

\medskip
\textbf{3.6.1.}\quad $C_m = \dfrac{e^{im\pi/L(x_0+\Delta/2)}}{m\pi\Delta}\sin\dfrac{m\pi\Delta}{2L}$

%% ══════════════════════════════════════════════════════════════
\section*{Chapter 4}
%% ══════════════════════════════════════════════════════════════

\textbf{4.4.1.}\quad (a) $\dfrac{n\pi c}{L}$, $n = 1, 2, 3, \ldots$

\quad (b) $(m - \frac{1}{2})\pi c/H$, $m = 1, 2, 3, \ldots$

\medskip
\textbf{4.4.2.}\quad (c) Frequencies of vibration are $\sqrt{\lambda_n} = \sqrt{\dfrac{(n\pi/L)^2 T_0 - \alpha}{\rho_0}}$

\medskip
\textbf{4.4.3.}\quad (b) $u = e^{-\beta t/2\rho_0}\sum_{n=1}^{\infty}(a_n\cos w_n t + b_n\sin w_n t)\sin\dfrac{n\pi x}{L}$, where
$w_n = \sqrt{\dfrac{T_0}{\rho_0}\left(\dfrac{n\pi}{L}\right)^2 - \dfrac{\beta^2}{4\rho_0^2}}$

%% ══════════════════════════════════════════════════════════════
\section*{Chapter 5}
%% ══════════════════════════════════════════════════════════════

\textbf{5.3.1.}\quad $T_0\odd{\phi}{x} + \alpha\phi + \lambda\rho_0\phi = 0$

\medskip
\textbf{5.3.3.}\quad $H = c_1\exp\!\left[\int^x \alpha(t)\,dt\right]$, let $c_1 = 1$. Then $p(x) = H$, $q(x) = \gamma H$, and $\sigma(x) = \beta H$.

\medskip
\textbf{5.3.4.}\quad (b) $u = e^{\frac{\alpha}{2k}x}\sum_{n=1}^{\infty} A_n\sin\dfrac{n\pi x}{L}e^{-\lambda_n t}$, where $A_n = \dfrac{2}{L}\int_0^L f(x)e^{-\frac{\alpha}{2k}x}\sin\dfrac{n\pi x}{L}\,dx$.

Note that $\lambda = \dfrac{\alpha^2}{4k} + k\left(\dfrac{n\pi}{L}\right)^2$.

\medskip
\textbf{5.3.9.}\quad (c) $n = 1, 2, \ldots$\,. $\lambda = (n\pi/\ln b)^2$

\medskip
\textbf{5.4.2.}\quad $u = \sum_{n=1}^{\infty} a_n\phi_n(x)e^{-\lambda_n t}$, where $a_n = \dfrac{\int_0^L f(x)\phi_n(x)r\,dr}{\int_0^L \phi_n^2(r)r\,dr}$

\medskip
\textbf{5.4.3.}\quad $u = \sum_{n=1}^{\infty} A_n\phi_n(r)e^{-\lambda_n t}$ (with $\lambda_1 = 0$, $\phi_1 = 1$)

\medskip
\textbf{5.4.6.}\quad $u = \sum_{n=1}^{\infty} A_n\phi_n(x)\cos\sqrt{\lambda_n}\,t$

\medskip
\textbf{5.5.1.}\quad (g) $\alpha\delta - \beta\gamma = 1$

\medskip
\textbf{5.5.9.}\quad $\lambda = \dfrac{-\int_0^L\left(\od{\phi}{x}\right)^2 dx}{\int_0^L e^x \phi^2\,dx}$

\medskip
\textbf{5.5.11.}\quad (a) $H(x) = \rho\left(u\od{v}{x} - v\od{u}{x}\right) + uv\left(\od{\rho}{x} - \tau\right)$% UNCLEAR: exact form, page 737

\medskip
\textbf{5.5A.5.}\quad (b) $\lambda = 2 \pm \sqrt{3}$

\medskip
\textbf{5.6.1.}\quad (c) $u_T = ax + bx^2$ with $a = 3$, $b = -2$, $\lambda_1 \leq 4\frac{1}{6}$

\medskip
\textbf{5.7.1.}\quad The circular frequency (cycles per $2\pi$ units of time) is $\sqrt{\lambda}$, the actual frequency is $\sqrt{\lambda}/2\pi$ (cycles per 1 unit of time):
\[
\frac{1}{2} \leq \frac{\sqrt{\lambda}}{2\pi} \leq \frac{1}{2}\sqrt{1+\alpha^2}.
\]

\medskip
\textbf{5.8.2.}\quad (c) By extension of Fig.~5.8.2a, $(n-l)\pi < \sqrt{\lambda_n}L < (n-\frac{1}{2})\pi$.

\medskip
\textbf{5.8.3.}\quad (b) $(n-1)\pi < \sqrt{\lambda_n}L < (n-\frac{1}{2})\,\pi$, $n = 1, 2, 3, \ldots$

\medskip
\textbf{5.8.7.}\quad (b) $\lambda = 1/4$ \quad (c) none \quad (d) no \quad (e) yes

\medskip
\textbf{5.8.8.}\quad (c) $\sqrt{\lambda_n} \sim (n-1)\pi$

\medskip
\textbf{5.8.10.}\quad (a) $\lambda_1 \approx 4.12$. \qquad (b) $\lambda_1 = 4.11586\ldots$

\medskip
\textbf{5.8.13.}\quad $\cos\sqrt{\lambda}L = \dfrac{1}{\sqrt{1+(\lambda/h^2)}}$. Thus $\int_0^L\sin^2\sqrt{\lambda}\,x\,dx = \dfrac{L}{2}+\dfrac{1}{2h[1+(\lambda/h^2)]}$.

\medskip
\textbf{5.9.1.}\quad (b) $\lambda^{1/2}\int_0^L (\sigma/p)^{1/2}\,dx_0 \approx (n+\frac{1}{2})\,\pi$

\medskip
\textbf{5.9.3.}\quad (a) $A'' + i\lambda^{1/2}\left(2A'\sigma^{1/2}+\frac{1}{2}\sigma^{-1/2}\sigma'A\right)+qA = 0$.

\quad (e) $A_{n+1} = \frac{1}{2}\sigma^{-1/4}\int_0^x \sigma^{-1/4}\left(A_n''+qA_n\right)\,dx_0$

\medskip
\textbf{5.10.2.}\quad (b) $\dfrac{\pi^4}{96} = 1 + \dfrac{1}{3^4} + \dfrac{1}{5^4} + \dfrac{1}{7^4} + \cdots$

%% ══════════════════════════════════════════════════════════════
\section*{Chapter 6}
%% ══════════════════════════════════════════════════════════════

\textbf{6.2.6.}\quad $\pdd{u}{x\,\partial y} \approx \dfrac{1}{4(\Delta x)^2}[u(x+\Delta x,\,y+\Delta y)-u(x-\Delta x,\,y+\Delta y)-u(x+\Delta x,\,y-\Delta y)+u(x-\Delta x,\,y-\Delta y)]$ assuming that $\Delta x = \Delta y$

\medskip
\textbf{6.3.4.}\quad (a) $\beta_n = \dfrac{\sum_{j=1}^{N-1}f_j\sin n\pi j/N}{\sum_{j=1}^{N-1}\sin^2 n\pi j/N}$.

\medskip
\textbf{6.3.4.}\quad (b) $\dfrac{N-1}{2}$

\medskip
\textbf{6.3.6.}\quad (d) Stable if $s < 0.5125$

\medskip
\textbf{6.3.9.}\quad (b) $\vec{A} = \dfrac{1}{(\Delta x)^2}\begin{bmatrix} -2 & 1 \\ 1 & -2 \end{bmatrix}$

\medskip
\textbf{6.3.10.}\quad (b) $\pd{u}{t} = \dfrac{ka}{\delta}\pdd{u}{x}$

\medskip
\textbf{6.3.14.}\quad (c) $\lambda = 0,\,3,\,4$; $|\lambda-1|\leq 5$, $|\lambda-4|\leq 8$, $|\lambda-2|\leq\frac{1}{3}$

\medskip
\textbf{6.4.1}\quad $k\Delta t\left[\dfrac{1}{(\Delta x)^2}+\dfrac{1}{(\Delta y)^2}\right]\leq\dfrac{1}{2}$

\medskip
\textbf{6.5.6.}\quad (b) Unstable

\medskip
\textbf{6.5.6.}\quad (b) Stable if $\dfrac{\Delta x\,c}{\Delta t}\leq 1$% UNCLEAR: 6.5.5 vs 6.5.6, page 738

%% ══════════════════════════════════════════════════════════════
\section*{Chapter 7}
%% ══════════════════════════════════════════════════════════════

\textbf{7.3.1.}\quad (a) $u = \sum_{n=1}^{\infty}\sum_{m=1}^{\infty} a_{nm}\sin\dfrac{n\pi x}{L}\sin\dfrac{m\pi y}{H}e^{-\lambda_{nm}kt}$,
where $\lambda_{nm} = \left(\dfrac{n\pi}{L}\right)^2+\left(\dfrac{m\pi}{H}\right)^2$

\quad (c)
\[
u = \sum\sum a_{nm}\cos\dfrac{n\pi x}{L}\sin\dfrac{m\pi y}{H}e^{-\lambda_{nm}kt},\quad\text{where } \lambda_{nm} = \left(\dfrac{n\pi}{L}\right)^2+\left(\dfrac{m\pi}{H}\right)^2
\]

\medskip
\textbf{7.3.2.}\quad (b) $u(x,y,z,t) = \sum_{n=0}^{\infty}\sum_{m=0}^{\infty}\sum_{\ell=0}^{\infty} a_{nm\ell}\cos\dfrac{n\pi x}{L}\cos\dfrac{m\pi y}{H}\cos\dfrac{\ell\pi z}{W}e^{-\lambda_{nm\ell}kt}$,
where $\lambda_{nm\ell} = \left(\dfrac{n\pi}{L}\right)^2+\left(\dfrac{m\pi}{H}\right)^2+\left(\dfrac{\ell\pi}{W}\right)^2$

$u(x,y,z,t) \to a_{000} = \dfrac{1}{LHW}\int_0^L\int_0^H\int_0^W f(x,y,z)\,dx\,dy\,dz$ as $t \to \infty$

\medskip
\textbf{7.3.4.}\quad (a) $\beta_n = \dfrac{\sum_{j=1}^{N-1}f_j\sin n\pi j/N}{\sum_{j=1}^{N-1}\sin^2 n\pi j/N}$

\quad (b)
\[
u(x,y,t) = \sum_{n=0}^{\infty}\sum_{m=0}^{\infty} A_{nm}\cos\dfrac{n\pi x}{L}\cos\dfrac{m\pi y}{H}h_{nm}(t)
\]

$h_{nm}(t) = \begin{cases} t & n=0,\,m=0 \\ \sin\omega_{nm}t & \text{otherwise,}\end{cases}$ where $\omega_{nm}^2 = c^2\pi^2\left[(n/L)^2+(m/H)^2\right]$

\[
A_{nm}h'_{nm}(0) = \dfrac{\int_0^H\int_0^L f\cos\dfrac{n\pi x}{L}\cos\dfrac{m\pi y}{H}\,dx\,dy}{\int_0^H\int_0^L\cos^2\dfrac{n\pi x}{L}\cos^2\dfrac{m\pi y}{H}\,dx\,dy}
\]

\medskip
\textbf{7.3.6.}\quad (b) $u = \sum_{n=1}^{\infty}\sum_{m=0}^{\infty} A_{nm}\sin\dfrac{n\pi x}{L}\sin\dfrac{m\pi z}{W}\cosh\sqrt{\lambda_{nm}}\,z$,
$\lambda_{nm} = (n\pi/L)^2+(m\pi/W)^2$

\medskip
\textbf{7.3.7.}\quad (c)(d) $u(x,y,z) = \sum_{n=0}^{\infty}\sum_{m=0}^{\infty} A_{nm}\cos\dfrac{n\pi x}{W}\cos\dfrac{m\pi z}{L}\cosh\beta_{nm}x$,
where $\beta_{nm}^2 = (n\pi/W)^2+(m\pi/H)^2$

For (c) a solution only exists if $\int_0^H\int_0^W f(y,z)\,dy\,dz = 0$,
in which case $A_{00}$ is arbitrary and otherwise
\[
A_{nm}\beta_{nm}\sinh\beta_{nm}L = \dfrac{\int_0^H\int_0^W f\cos\dfrac{n\pi z}{W}\cos\dfrac{m\pi z}{L}\,dy\,dz}{\int_0^H\int_0^W\cos^2\dfrac{n\pi z}{W}\cos^2\dfrac{m\pi z}{L}\,dy\,dz}.
\]

For (d) $A_{nm}\cosh\beta_{nm}L = \dfrac{\int_0^H\int_0^W g\cos\dfrac{n\pi z}{W}\cos\dfrac{m\pi z}{L}\,dy\,dz}{\int_0^H\int_0^W\cos^2\dfrac{n\pi z}{W}\cos^2\dfrac{m\pi z}{L}\,dy\,dz}$.

\medskip
\textbf{7.4.1.}\quad (a) $\lambda_{nm} = (n\pi/H)^2+(m\pi/L)^2$, where $n=1,2,3,\ldots$ and $m=0,1,2,\ldots$

\medskip
\textbf{7.7.1.}\quad $u(r,\theta,t) = \sum_{n=1}^{\infty}A_n J_3(\sqrt{\lambda_{3n}}\,r)\sin 3\theta\sin c\sqrt{\lambda_{3n}}\,t$

\medskip
\textbf{7.7.2.}\quad (d) $u(r,\theta,t) = \sum_{m=0}^{\infty}\sum_{n=1}^{\infty}(A_{mn}\cos m\theta + B_{mn}\sin m\theta)\,H_{mn}(r,t)$, where

$H_{mn}(r,t) = \begin{cases} t & m=0,\,n=1 \\ J_m(\sqrt{\lambda_{mn}}\,r)\sin c\sqrt{\lambda_{mn}}\,t & \text{otherwise with } J_m'(\sqrt{\lambda_{mn}}\,a) = 0 \end{cases}$

\[
A_{mn} = \dfrac{\int_{-\pi}^{\pi}\int_0^a \beta(r,\theta)\phi_{mn}(r)\cos m\theta\,r\,dr\,d\theta}{\int_{-\pi}^{\pi}\int_0^a \phi_{mn}^2(r)\cos^2 m\theta\,r\,dr\,d\theta}
\]
and $B_{mn}$ is the same as $A_{mn}$ above replaced with $\sin m\theta$.

\medskip
\textbf{7.7.3.}\quad (a) $c\sqrt{\lambda_{mn}}$, where $J_{2m}(\sqrt{\lambda_{mn}}\,a) = 0$

\medskip
\textbf{7.7.5.}\quad The frequencies are $c\sqrt{\lambda}$,
where $J_{2m}\!\left(\sqrt{\lambda}\,a\right) Y_{2m}\!\left(\sqrt{\lambda}\,b\right) - J_{2m}\!\left(\sqrt{\lambda}\,b\right) Y_{2m}\!\left(\sqrt{\lambda}\,a\right) = 0$.

\medskip
\textbf{7.7.8.}\quad For the heat equation, Sec.~7.7 is valid with (7.7.5) replaced by $dh/dt = -\lambda kh$.
The boundary condition introduces more substantial changes. The Rayleigh quotient shows $\lambda \geq 0$ with $\lambda = 0$ only when $\phi(r,\theta)$ is constant, which means $m=0$. The other eigenfunctions still satisfy (7.7.38) with the boundary condition $f'(a) = 0$ yielding $J_m'(\sqrt{\lambda}\,a) = 0$. Thus

$\phi_{mn}(r,\theta) = \begin{cases} 1 & m=0,\,n=1 \\ J_m(\sqrt{\lambda}\,r)\cos m\theta & \text{otherwise.}\end{cases}$

As $t\to\infty$, $e^{-\lambda_{mn}kt}\to 0$ except for $m=0$, $n=1$ since then $\lambda_{mn}=0$. Thus $u(r,\theta,t)\to A_{01}$, where $A_{01} = \left(\int_{-\pi}^{\pi}\int_0^a f(r,\theta)r\,dr\,d\theta\right)/(\pi a^2)$.

\medskip
\textbf{7.7.9.}\quad (b)
$u(r,\theta,t) = \sum\sum A_{mn}\phi_{mn}(r)\cos m\theta e^{-\lambda_{mn}kt}$, where

$\phi_{mn}(r) = \begin{cases} 1 & m=0,\,n=1 \\ J_m(\sqrt{\lambda_{mn}}\,r) & \text{otherwise, where } J_m'(\sqrt{\lambda}\,a)=0 \end{cases}$

\medskip
\textbf{7.7.10.}\quad $u(r,\theta,t) = \sum_{n=1}^{\infty} a_n J_0(\sqrt{\lambda_n}\,r)\,e^{-\lambda_n kt}$, where $J_0(\sqrt{\lambda_n}\,a)=0$

\medskip
\textbf{7.7.12.}\quad (c) $y = c_1[\cos(2\ln x) + \ldots] + c_2[\sin(2\ln x) + \ldots]$

\quad (e) $y = c_1(x^2 + \ldots) + c_2(x^3 + \ldots)$

\medskip
\textbf{7.8.1.}\quad (b) $J_m(\sqrt{\lambda})\,Y_{2m}(2\sqrt{\lambda}) - J_{2m}(2\sqrt{\lambda})\,Y_m(\sqrt{\lambda}) = 0$

\medskip
\textbf{7.8.1.}\quad (d) $\frac{1}{2}n^2 \leq \lambda_1 \leq 2n^2$% UNCLEAR: exact inequality, page 740

\medskip
\textbf{7.8.2.}\quad (d) $u(r,\theta,t) = \sum\sum c_{mn}J_{2m}(\sqrt{\lambda_{mn}}\,r)\sin 2m\theta e^{-\lambda_{mn}kt}$, where $J_{2m}(\sqrt{\lambda_{mn}}\,a)=0$

\medskip
\textbf{7.8.8.}\quad $J_{1/2}(z) = \sqrt{2/\pi z}\sin z$

\medskip
\textbf{7.9.1.}\quad (b) $u(r,\theta,z) = \sum_{n=1}^{\infty}A_n\sinh\sqrt{\lambda_7{n}}(H-z)\sin 7\theta J_7(\sqrt{\lambda_{7n}}\,r)$, where $J_7(\sqrt{\lambda_{7n}}\,a) = 0$% UNCLEAR: exact subscript, page 740

\medskip
\textbf{7.9.2.}\quad (b) $u(r,\theta,z) = \sum_{m=1}^{\infty}\sum_{n=1}^{\infty}A_{mn}I_m\left[(n-\frac{1}{2})\frac{\pi}{H}r\right]\sin\left(n-\frac{1}{2}\right)\frac{\pi}{H}r\sin\left(n-\frac{1}{2}\right)\frac{\pi z}{H}\sin m\theta$% UNCLEAR: exact form, page 740

\medskip
\textbf{7.9.3.}\quad (b) $u(r,\theta,z,t) = \sum_{n=1}^{\infty}\sum_{m=0}^{\infty}\sum_{\ell=0}^{\infty}A_{\ell mn}\phi_{\ell mn}(r,\theta,z)e^{-\lambda_{mn}kt}$, where $\lambda_{mn} = (\ell\pi/H)^2 + \lambda'_{mn}$ and $J'_{2m}(\sqrt{\lambda'_{mn}}\,a) = 0$

$\phi_{\ell mn}(r,\theta,z) = \begin{cases} 1 & m=0,\,\ell=0,\,n=1 \\ \cos\dfrac{\ell\pi z}{H}\cos 2m\theta\, J_{2m}(\sqrt{\lambda'_{mn}}\,r) & \text{otherwise} \end{cases}$

\medskip
\textbf{7.9.4.}\quad (a) $u(r,z,t) = \sum_{m=1}^{\infty}\sum_{n=1}^{\infty}A_{nm}J_0(\sqrt{\lambda_n'}\,r)\sin\dfrac{m\pi z}{H}e^{-\lambda kt}$, where $\lambda = \lambda'+\left(\dfrac{m\pi}{H}\right)^2$ and $J_0(\sqrt{\lambda_n'}\,a)=0$. Here,
\[
A_{nm} = \dfrac{\iint f(r,z)J_0(\sqrt{\lambda_n'}\,r)\sin\dfrac{m\pi z}{H}\,r\,dr\,dz}{\iint J_0^2(\sqrt{\lambda_n'}\,r)\sin^2\dfrac{m\pi z}{H}\,r\,dr\,dz}.
\]

%% ══════════════════════════════════════════════════════════════
\section*{Chapter 8}
%% ══════════════════════════════════════════════════════════════

\textbf{8.2.1.}\quad (a) $u(x,t) = A + Bx + \sum_{n=1}^{\infty}a_n\sin\left(n-\frac{1}{2}\right)\dfrac{\pi x}{L}\,e^{-[(n-1/2)\pi/L]^2 kt}$

where $a_n = \dfrac{2}{L}\int_0^L g(x)\sin\left(n-\frac{1}{2}\right)\dfrac{\pi x}{L}\,dx$

\quad (d) $u_E(x) = -\dfrac{x^2}{2}+\left(\dfrac{B-A}{L}+\dfrac{L}{2}\right)x+A$.

\medskip
\textbf{8.2.2.}\quad (a) $r(x,t) = A(t)x + B(t) - LA(t)$

\quad (c) $r(x,t) = A(t)x + B(t) - LA(t)$

\medskip
\textbf{8.2.6.}\quad (a) $u(x,t) = A + (B-A)\dfrac{x}{L} + \sum_{n=1}^{\infty}\sin\dfrac{n\pi x}{L}\left(A_n\cos\dfrac{n\pi ct}{L}+B_n\sin\dfrac{n\pi ct}{L}\right)$,
where $A_n = \dfrac{2}{L}\int_0^L\{f(x)-[A+(B-A)\frac{x}{L}]\}\sin\dfrac{n\pi x}{L}\,dx$ and
$B_n = \dfrac{1}{n\pi c}\int_0^L g(x)\sin\dfrac{n\pi x}{L}\,dx$

\quad (d) $u_E(x) = \dfrac{L^2}{c^2_{3n-1}}\sin\dfrac{\pi x}{L}$% UNCLEAR: exact form, page 741

\medskip
\textbf{8.3.1.}\quad (c) $u(x,t) = \sum_{n=0}^{\infty}A_n(t)\cos\dfrac{n\pi x}{L}$,

\quad (f) $u(x,t) = \sum_{n=0}^{\infty}A_n(t)\cos\dfrac{n\pi x}{L}$,
\[
\od{A_n}{t} + k\left(\dfrac{n\pi}{L}\right)^2 A_n = \dfrac{\int_0^L Q(x,t)\cos n\pi x/L\,dx}{\int_0^L\cos^2 n\pi x/L\,dx}
\]

\medskip
\textbf{8.3.3.}\quad $u(x,t) = \sum_{n=1}^{\infty}a_n(t)\phi_n(x)$, where $\od{a_n}{t} = -\lambda_n a_n + \dfrac{\int_0^L \phi_n f\,dx}{\int_0^L \phi_n^2 c\rho\,dx}$

\medskip
\textbf{8.3.4.}\quad (a) $u_E(x) = A + (B-A)\dfrac{\int_0^x dz/K_0(z)}{\int_0^L dz/K_0(z)}$

\medskip
\textbf{8.3.5.}\quad $u(r,t) = \sum_{n=1}^{\infty}A_n(t)J_0(\lambda_n^{1/2}r)$, where $J_0(\lambda_n^{1/2}a) = 0$

and where $\od{A_n}{t} + k\lambda_n A_n = \dfrac{\int_0^a f(r,t)J_0(\lambda_n^{1/2}r)r\,dr}{\int_0^a J_0^2(\lambda_n^{1/2}r)r\,dr}$

\medskip
\textbf{8.3.7.}\quad $r(x,t) = \dfrac{x^3}{6L}+\left(\dfrac{1}{L}-\dfrac{1}{6}\right)x$

\medskip
\textbf{8.4.1.}\quad (b) $u(x,t) = \sum_{n=0}^{\infty}A_n(t)\cos\dfrac{n\pi x}{L}$, where

$A_n(t) = e^{-k(n\pi/L)^2t}\left(A_n(0) + \int e^{k(n\pi/L)^2\bar t}\left\{q_n(\bar t) + \dfrac{Lnk}{L}\left[(-1)^n B(\bar t)-A(\bar t)\right]\right\}\,d\bar t\right)$

with $A_n(0) = \dfrac{\int_0^L f(x)\cos\dfrac{n\pi x}{L}\,dx}{\int_0^L\cos^2\dfrac{n\pi x}{L}\,dx}$ and $q_n(t) = \dfrac{\int_0^L Q(x,t)\cos\dfrac{n\pi x}{L}\,dx}{\int_0^L\cos^2\dfrac{n\pi x}{L}\,dx}$

and $I_n = \begin{cases} 1 & n=0 \\ 2 & n \neq 0 \end{cases}$

\medskip
\textbf{8.5.2.}\quad (b) $\omega^2 = \left(\dfrac{n\pi c}{L}\right)^2$

\medskip
\textbf{8.5.5.}\quad (c) $u(r,\theta,t) = \sum_{m=1}^{\infty}\sum_{n=1}^{\infty}A_{nm}(t)J_m(\sqrt{\lambda_n}\,r)\sin m\theta$ with $J_m(\sqrt{\lambda}\,a) = 0$,
where $A_{nm} = \dfrac{1}{c\sqrt{\lambda_n}}\int_0^t Q_{nm}(\bar t)\sin c\sqrt{\lambda_n}(t-\bar t)\,d\bar t + c_{nm}\cos c\sqrt{\lambda_n}\,t$,

$Q_{nm} = \dfrac{\iint Q(x,y,t)J_m(\sqrt{\lambda_n}\,r)\sin m\theta\,r\,dr\,d\theta}{\iint J_m^2(\sqrt{\lambda_n}\,r)\sin^2 m\theta\,r\,dr\,d\theta}$,\quad $c_{nm} = \dfrac{\iint f(x,y)J_m(\sqrt{\lambda_n}\,r)\sin m\theta\,r\,dr\,d\theta}{\iint J_m^2(\sqrt{\lambda_n}\,r)\sin^2 m\theta\,r\,dr\,d\theta}$

\medskip
\textbf{8.5.6.}\quad (a) $\dfrac{1}{r}\frac{d}{d r}\left(r\od{\phi}{r}\right)+\lambda a = \dfrac{g\phi r\,dr\,d\theta}{\int_0^a\phi^2 r\,dr\,d\theta}$, where $\frac{d}{d r}J_m(\lambda^{1/2}a) = 0$% UNCLEAR: exact form, page 742

\medskip
\textbf{8.6.1.}\quad (b) $u(x,y) = \sum_{n=1}^{\infty}\sum_{m=1}^{\infty}A_{nm}\sin\dfrac{n\pi x}{L}\sin\dfrac{m\pi y}{H}$, where
$A_{nm} = \dfrac{-Q_{nm}-(2/L)(-1)^n(n\pi/L)}{(n\pi/L)^2+(m\pi/H)^2}$

\quad (d) If $\iint Q\,dx\,dy = 0$, then $u = \sum_{n=0}^{\infty}\sum_{m=0}^{\infty}A_{nm}\cos\dfrac{n\pi x}{L}\cos\dfrac{m\pi y}{H}$, where
$A_{00}$ is arbitrary and the others are given by

\[
-A_{nm}\left[\left(\dfrac{n\pi}{L}\right)^2+\left(\dfrac{m\pi}{H}\right)^2\right] = \dfrac{\iint Q\cos\dfrac{n\pi x}{L}\cos\dfrac{m\pi y}{H}\,dx\,dy}{\iint\cos^2\dfrac{n\pi x}{L}\cos^2\dfrac{m\pi y}{H}\,dx\,dy}
\]

\medskip
\textbf{8.6.3.}\quad (a) $u(r,\theta) = \sum_{m=0}^{\infty}\sum_{n=1}^{\infty}A_{mn}\cos m\theta J_m(\sqrt{\lambda_n}\,r) + \sum_{m=1}^{\infty}\sum_{n=1}^{\infty}B_{mn}\sin m\theta J_m(\sqrt{\lambda_n}\,r)$,

where $J_m(\sqrt{\lambda}\,a) = 0$ and $\begin{pmatrix}A_{mn}\\B_{mn}\end{pmatrix} = \dfrac{-\dfrac{1}{\lambda}\iint Q\begin{pmatrix}\cos m\theta\\\sin m\theta\end{pmatrix}J_m(\sqrt{\lambda_n}\,r)r\,dr\,d\theta}{\iint\begin{pmatrix}\cos^2 m\theta\\\sin^2 m\theta\end{pmatrix}J_m^2(\sqrt{\lambda_n}\,r)r\,dr\,d\theta}$

\medskip
\textbf{8.6.6.}\quad $u(x,y) = \sum_{n=1}^{\infty}a_n(y)\sin nx$, where $a_n(y) = \frac{1}{3}e^{2y}\delta_{n,1} + \alpha_n\sinh ny + \beta_n\cosh ny$

and $\delta_{n,1} = \begin{cases} 1 & n=1 \\ 0 & n \neq 1 \end{cases}$

%% ══════════════════════════════════════════════════════════════
\section*{Chapter 9}
%% ══════════════════════════════════════════════════════════════

\textbf{9.2.1.}\quad (d) $G(x,t;x_0,0) = \sum_{n=0}^{\infty}\dfrac{1}{I_n}\cos\dfrac{n\pi x}{L}\cos\dfrac{n\pi x_0}{L}e^{-k(n\pi/L)^2(t-t_0)}$,

where $I_n = \begin{cases} L & n=0 \\ L/2 & n\neq 0 \end{cases}$

$u(x,t) = \int g(x_0)G(x,t;x_0,t_0)\,dx_0 + \int_0^L\int_0^t Q(x_0,t_0)G(x,t;x_0,t_0)\,dt_0\,dx_0$

$\quad + \int kB(t_0)G(x,t;L,t_0)\,dt_0 - \int kA(t)G(x,t;0,t_0)\,dt_0$

\medskip
\textbf{9.2.3.}\quad $G(x,t;x_0,t_0) = \sum_{n=1}^{\infty}\dfrac{2}{L}\sin\dfrac{n\pi x_0}{L}\sin\dfrac{n\pi x}{L}\dfrac{\sin n\pi c(t-t_0)/L}{n\pi c/L}$

$u(x,t) = \int_0^L\int_0^t Q(x_0,t_0)G(x,t;x_0,t_0)\,dt_0\,dx_0 + \int_0^L g(x_0)G(x,t;x_0,0)\,dx_0$

$\quad + \int_0^L f(x_0)\pd{G}{t}(x,t;x_0,0)\,dx_0$% UNCLEAR: some terms, page 743

\medskip
\textbf{9.3.5.}\quad (a), (b) $u(x) = \int_0^x (x-x_0)f(x_0)\,dx_0 - x\int_0^L f(x_0)\,dx_0$

\medskip
\textbf{9.3.5.}\quad (c) $G(x,x_0) = \begin{cases} -x & x < x_0 \\ -x_0 & x > x_0 \end{cases}$

\medskip
\textbf{9.3.6.}\quad (a) See answer to 9.3.5(c).

\medskip
\textbf{9.3.9.}\quad (b) See answer to 9.3.11.

\medskip
\textbf{9.3.11.}\quad (a) $G(x,x_0) = \begin{cases} \dfrac{\sin(x_0-L)\sin x}{\sin L} & x < x_0 \\[6pt] \dfrac{\sin(x-L)\sin x_0}{\sin L} & x > x_0 \end{cases}$

\medskip
\textbf{9.3.13.}\quad (b) $G(x,x_0) = \dfrac{1}{2ik}e^{ik|x-x_0|}$

\medskip
\textbf{9.3.14.}\quad (d) $u(x) = \int_0^L G(x,x_0)f(x_0)\,dx_0 - \alpha p(0)\left.\od{G}{x_0}\right|_{x_0=0}(x,x_0) - \beta p(L)G(x,L)$% UNCLEAR: exact boundary term, page 743

\medskip
\textbf{9.3.15.}\quad (a) $G(x,x_0) = \begin{cases} \frac{1}{k}y_1(x)y_2(x_0) & x < x_0 \\ \frac{1}{k}y_1(x_0)y_2(x) & x > x_0, \end{cases}$ where $k$ is a constant

\medskip
\textbf{9.3.21.}\quad $G(x,x_0) = \begin{cases} 0 & x < x_0 \\ 1 & x > x_0 \end{cases}$

\medskip
\textbf{9.3.25.}\quad (b) $u(x) = \frac{1}{6}\int_0^x f(t_0)(x-t_0)^3\,dt_0$

\medskip
\textbf{9.4.2.}\quad (a) $0 = \int_0^L \phi_h(x)f(x)\,dx - \alpha p(0)\od{\phi_h}{x}\Big|_{x=0} + \beta p(L)\od{\phi_h}{x}\Big|_{x=L}$

\medskip
\textbf{9.4.3.}\quad (b) Infinite number of solutions

\medskip
\textbf{9.4.6.}\quad (a) $u = 1 + c_1\cos x + c_2\sin x$; no solutions

\quad (b) $c_2 = 0$, $c_1$ arbitrary

\quad (c) $c_1$ and $c_2$ arbitrary

\medskip
\textbf{9.4.8.}\quad (a) $u = \frac{1}{2}x\sin x + c_2\sin x$

\medskip
\textbf{9.4.10.}\quad $G_m(x,x_0) = a\sin x\sin x_0 + \frac{1}{2}(x\cos x\sin x_0 + x\cos x\sin x_0) - \cos x_0\sin x$,

$\alpha$ is an arbitrary constant

\medskip
\textbf{9.4.11.}\quad (a),(b) $c = 1$

\quad (d) $G_a(x,x_0) = \alpha + \begin{cases} x_0 & x < x_0 \\ x & x > x_0, \end{cases}$ where $\alpha$ is an arbitrary constant

\quad (e) $u(x) = \int_0^L f(x_0)G_a(x,x_0)\,dx_0 + k_1$, where $k_1$ is arbitrary

\medskip
\textbf{9.5.3.}\quad (c) $G(r,\theta;r_0,\theta_0) = \sum_{m=1}^{\infty}\sum_{n=1}^{\infty}\dfrac{\sin m\theta\sin m\theta_0}{\lambda}\dfrac{J_m(\sqrt{\lambda}\,r)J_m(\sqrt{\lambda}\,r_0)}{-\lambda\iint J_m^2(\sqrt{\lambda}\,r)\sin^2 m\theta\,r\,dr\,d\theta}$,

where $J_m(\sqrt{\lambda}\,a) = 0$

\medskip
\textbf{9.5.4.}\quad See (9.5.23) with extra integral signs

\medskip
\textbf{9.5.9.}\quad (b) $G(x,x_0) = \sum_{m=1}^{\infty}\dfrac{\sin m\theta_0\sin m\theta}{m\pi}\left\{\begin{array}{ll}\left(\frac{r}{r_0}\right)^m\left[\left(\frac{r_0}{a}\right)^m-\left(\frac{a}{r_0}\right)^m\right] & r < r_0\\[6pt] \left(\frac{r_0}{r}\right)^m\left[\left(\frac{r}{a}\right)^m-\left(\frac{a}{r}\right)^m\right] & r > r_0\end{array}\right.$% UNCLEAR: exact form, page 744

\medskip
\textbf{9.5.10.}\quad (a) $L = \nabla^2 + k^2$, $\iiint[uL(v)-vL(u)]\,dV = \oint(u\nabla v - v\nabla u)\cdot\vec{n}\,dS$

\quad (b) $c_2 = 0$, $c_1 = \dfrac{1}{4\pi}$ \qquad (c) $G = \frac{1}{4}\left[Y_0(kr) - iJ_0(kr)\right]$

\medskip
\textbf{9.5.13.}\quad (a) $G(\vec{x},\vec{x}_0) = -\dfrac{1}{4\pi}\left[\dfrac{1}{\sqrt{(x-x_0)^2+(y-y_0)^2+(z-z_0)^2}} + \dfrac{1}{\sqrt{(x-x_0)^2+(y+y_0)^2+(z-z_0)^2}}\right]$% UNCLEAR: sign, page 745

\medskip
\textbf{9.5.14.}\quad $G(\vec{x},\vec{x}_0) = \dfrac{1}{4\pi}\ln\dfrac{[(x-x_0)^2+(y-y_0)^2][(x+x_0)^2+(y+y_0)^2]}{[(x-x_0)^2+(y+y_0)^2][(x+x_0)^2+(y-y_0)^2]}$% UNCLEAR: exact form, page 745

\medskip
\textbf{9.5.19.}\quad $G(\vec{x},\vec{x}_0) = \frac{1}{4\pi}\ln\left[a^2\,\dfrac{r^2+r_0^2-2rr_0\cos(\theta-\theta_0)}{r^2r_0^2+a^4-2rr_0a^2\cos(\theta-\theta_0)}\right]-\frac{1}{4\pi}\ln\left[a^2\,\dfrac{r^2+r_0^2-2rr_0\cos(\theta+\theta_0)}{r^2r_0^2+a^4-2rr_0a^2\cos(\theta+\theta_0)}\right]$% UNCLEAR: exact form, page 745

\medskip
\textbf{9.5.22.}\quad (c) $G(\vec{x},\vec{x}_0) = -\dfrac{1}{4\pi}\sum_{n=-\infty}^{\infty}\left(\dfrac{1}{|\vec{x}-\vec{\alpha}_n|}-\dfrac{1}{|\vec{x}-\vec{\beta}_n|}\right)$,

where $\vec{\alpha}_n = (x_0+2Ln,\,y_0,z_0)$ and $\vec{\beta}_n = (-x_0+2Ln,\,y_0,z_0)$

%% ══════════════════════════════════════════════════════════════
\section*{Chapter 10}
%% ══════════════════════════════════════════════════════════════

\textbf{10.2.1.}\quad $c(\omega) = \begin{cases}\frac{1}{2}[A(-\omega)-iB(-\omega)] & \omega < 0 \\[4pt] \frac{1}{2}[A(\omega)+iB(\omega)] & \omega > 0\end{cases}$

\medskip
\textbf{10.3.6.}\quad See Table 10.4.1

\medskip
\textbf{10.3.7.}\quad See Table 10.4.1

\medskip
\textbf{10.3.10.}\quad (b) $\int_0^L J_0(sr)J_0(s_1 r)r\,dr$

\quad (c) $A(s_1) = \int_0^{\infty}f(r)J_0(s_1 r)r\,dr$

\medskip
\textbf{10.3.10.}\quad $\approx \dfrac{2}{\pi}\,\dfrac{-\sqrt{s_1/s}\cos sL-\pi/4\sin s_1 L-\pi/4+\sqrt{s_1/s}\cos s_1 L-\pi/4\sin sL-\pi/4}{s^2-s_1^2}$% UNCLEAR: exact form, page 745

\medskip
\textbf{10.3.16.}\quad $\int_0^{\infty}y^p e^{-ky}\,dy = \frac{1}{k}k^{-(1+p)/\pi}\Gamma\!\left(\frac{1+p}{\pi}\right)$% UNCLEAR: exact formula, page 745

\medskip
\textbf{10.4.3.}\quad (a) $u(x,t) = \dfrac{1}{\sqrt{4\pi kt}}\int_{-\infty}^{\infty}f(\bar{x})e^{-(x+ct-\bar{x})^2/4kt}\,d\bar{x}$

\medskip
\textbf{10.4.5.}\quad (a) $u(x,t) = \dfrac{1}{2\pi}\int_{-\infty}^{\infty}f(\bar{x})\sqrt{\dfrac{\pi}{kt}}e^{-(x-\bar{x})^2/4kt}\,d\bar{x}$% UNCLEAR: exact form, page 745

\medskip
\textbf{10.4.6.}\quad $\text{Ai}(x) = \dfrac{1}{\pi}\int_0^{\infty}\cos\left(\dfrac{\omega^3}{3}+\omega x\right)\,d\omega$

\medskip
\textbf{10.4.7.}\quad (c) $u(x,t) = \dfrac{1}{(3kt)^{1/3}}\int_{-\infty}^{\infty}f(\bar{x})\,\text{Ai}\!\left[\dfrac{x}{(3kt)^{1/3}}\right]$% UNCLEAR: argument of Ai, page 745

\medskip
\textbf{10.5.3.}\quad $C[e^{-\alpha x^2}] = 2\dfrac{1}{\sqrt{4\pi\alpha}}e^{-\omega^2/4\alpha}$

\medskip
\textbf{10.5.10.}\quad $C^{-1}(\omega e^{-\omega\alpha}) = \dfrac{(a^2-x^2)}{(\alpha^2+x^2)^2}$% UNCLEAR: exact formula, page 745

\medskip
\textbf{10.5.17.}\quad (b) $\bar{U}(\omega,t) \approx \dfrac{(2/\pi)\omega A e^{i\sigma_0 t}}{\omega^2+i\sigma_0/k}$

\medskip
\textbf{10.6.1.}\quad (a) $\bar{u}(\omega,y) = F_2(\omega)\dfrac{\sinh\omega y}{\sinh\omega H} + F_1(\omega)\dfrac{\sinh\omega(H-y)}{\sinh\omega H}$, where $\bar{u}(\omega,y)$ is the Fourier transform of $u(x,y)$

\medskip
\textbf{10.6.2.}\quad (b) $\bar{u}(x,\omega) = \dfrac{G_1(\omega)\cosh\omega(L-x)}{\cosh\omega L}$, where $\bar{u}(x,\omega)$ is the cosine transform of $u(x,y)$

\medskip
\textbf{10.6.4.}\quad (a) $u(x,t) = \dfrac{1}{2\pi}\int_0^{\infty}f(\bar{x})\ln\dfrac{(x-\bar{x})^2+y^2}{(x+\bar{x})^2+y^2}\,d\bar{x}$% UNCLEAR: exact form, page 745

\medskip
\textbf{10.6.11.}\quad (a) $u(x,y,t) = \int_0^{\infty}\int_0^{\infty}f(x_0,y_0)\dfrac{1}{4\pi kt}\left\{\exp\left[\dfrac{-(x-x_0)^2-(y-y_0)^2}{4kt}\right]\right.$

$\left.+\exp\left[\dfrac{-(x+x_0)^2-(y+y_0)^2}{4kt}\right] - \exp\left[\dfrac{-(x+x_0)^2-(y-y_0)^2}{4kt}\right]\right.$

$\left.-\exp\left[\dfrac{-(x-x_0)^2-(y+y_0)^2}{4kt}\right]\right\}\,dx_0\,dy_0$

\medskip
\textbf{10.6.12.}\quad (a) $u(x,y,t) = \int_0^{\infty}\sum_{n=1}^{\infty}\bar{A}_n(\omega,t)\sin\dfrac{n\pi y}{H}\sin\omega y\,d\omega$, where

$\bar{A}_n(\omega,t) = c(\omega)e^{-k[\omega^2+(n\pi/L)^2]t}$

and

$c(\omega) = \dfrac{4}{L}\int_0^{\infty}\int_0^L f(x,y)\sin\dfrac{n\pi y}{H}\sin\omega y\,dx\,dy$% UNCLEAR: variable names, page 746

\medskip
\textbf{10.6.15.}\quad (a) $\bar{U} = F(\omega)e^{-\omega x}$

\quad (b) $u(x,y) = \dfrac{x}{2\pi}\int_{-\infty}^{\infty}\int_{-\infty}^{\infty}\dfrac{f(x_0,y_0)\,dx_0\,dy_0}{[(x-x_0)^2+(y-y_0)^2+x^2]^{3/2}}$% UNCLEAR: exact form, page 746

\medskip
\textbf{10.6.16.}\quad (b) $u(r,\theta) = \int_0^{\infty}A(\omega)\sinh\omega\theta\sin\left(\omega\ln\frac{r}{a}\right)\,d\omega$% UNCLEAR: exact form, page 746

\medskip
\textbf{10.6.18.}\quad $u(x,t) = \dfrac{1}{2c}\int_{x-ct}^{x+ct}g(\bar{x})\,d\bar{x}$

%% ══════════════════════════════════════════════════════════════
\section*{Chapter 11}
%% ══════════════════════════════════════════════════════════════

\textbf{11.2.6.}\quad (b) % UNCLEAR: graphical answer showing characteristics, page 746

\medskip
\textbf{11.2.7.}\quad (a) $u(x,t) = \int_{x-ct}^{x+ct}g(x_0)\dfrac{1-e^{-i\omega(t-|x-x_0|/c)}}{2i\omega c}\,dx_0$

\medskip
\textbf{11.2.8.}\quad (a) Influence function $= \begin{cases} 0 & \text{if } |x-x_0| > ct \\[4pt] \dfrac{e^{-i\omega(t-r/c)}}{4\pi c^2 r} & \text{if } |x-x_0| < ct, \end{cases}$ where $r = |x-x_0|$

\medskip
\textbf{11.2.10.}\quad (b) $u(x,t) = \begin{cases} 0 & \text{if } x > ct \\ h\left(t - \frac{x}{c}\right) & \text{if } x < ct \end{cases}$

\medskip
\textbf{11.2.12.}\quad (a) $G(x,t;x_1,t_1) = \begin{cases} 0 & \text{if } r > c(t-t_1) \\[4pt] \dfrac{1}{2\pi c\sqrt{c^2(t-t_1)^2-r^2}} & \text{if } r < c(t-t_1) \end{cases}$

\quad (c) If $A=1$, $u(x,t) = \dfrac{2}{\sqrt{\pi}}\int_{x/\sqrt{4kt}}^{\infty}e^{-\eta^2}\,d\eta$

\medskip
\textbf{11.3.2.}\quad $G(t,t_0) = \sin(t-t_0)$

\medskip
\textbf{11.3.3.}\quad $G(x,t;x_0,t_0) = \dfrac{1}{\sqrt{4\pi k(t-t_0)}}\left\{\exp\left[\dfrac{-(x-x_0)^2}{4k(t-t_0)}\right]+\exp\left[\dfrac{-(x+x_0)^2}{4k(t-t_0)}\right]\right\}$

%% ══════════════════════════════════════════════════════════════
\section*{Chapter 12}
%% ══════════════════════════════════════════════════════════════

\textbf{12.2.2.}\quad $w(x,t) = \cos(x+3t)$

\medskip
\textbf{12.2.5.}\quad (b) $w(x,t) = t + f(xe^{-t})$

\quad (d) $w(x,t) = e^t f(x-\frac{3}{2}t^2)$

\medskip
\textbf{12.2.6.}\quad $x = 2f(x_0)t + x_0$

\medskip
\textbf{12.2.8.}\quad $u(x,t) = \begin{cases} 1 & x \leq 2t \\ \frac{x}{2t} & 2t < x < 4t \\ 2 & x \geq 4t \end{cases}$

\medskip
\textbf{12.3.4.}\quad (a) $\pd{u}{t}(x,0) = -c\od{F}{x}(x)$

\quad (b) $\pd{u}{t}(0,t) = -\dfrac{1}{c}\od{F(-ct)}{t}$% UNCLEAR: exact form, page 747

\medskip
\textbf{12.4.1.}\quad $u(x,t) = \begin{cases} 0 & \text{if } x > ct \\ h\left(t - \frac{x}{c}\right) & \text{if } x < ct \end{cases}$

\medskip
\textbf{12.4.2.}\quad $u(x,t) = \begin{cases} \cos x\cos ct & \text{if } x < -ct \\ e^{-(t+x/c)} + \sin x\sin ct & 0 > x > -ct \end{cases}$

\medskip
\textbf{12.4.6.}\quad $u(x,t) = \begin{cases} 0 & x > ct \\ -c\int_0^{t-x/c}h(\bar{t})\,d\bar{t} & x < ct \end{cases}$

\medskip
\textbf{12.4.7.}\quad $u(x,t) = \begin{cases} \frac{1}{2}[f(x-ct)+f(x+ct)] & \text{if } x > ct \\ \frac{1}{2}[f(x+ct)+f(ct-x)] - c\int_0^{t-x/c}h(\bar{t})\,d\bar{t} & \text{if } x < ct \end{cases}$

\medskip
\textbf{12.5.1.}\quad (b) $u(x,t) = \frac{1}{2}[f(x-ct)+f(x+ct)]$

\medskip
\textbf{12.6.1.}\quad (a) $\rho(x,t) = f(x)$

\quad (c) $\rho(x,t) = f(x)e^{-3xt}$% UNCLEAR: exact exponent, page 747

\medskip
\textbf{12.6.2.}\quad $\rho(x,t) = (1+\sin x)e^{t+x/2}$% UNCLEAR: exact exponent, page 747

\medskip
\textbf{12.6.3.}\quad (a) $\rho(x,t) = \sin(x - c_0 t)$

\quad (b) $\rho(x,t) = \begin{cases} g\left(t - \frac{x}{c_0}\right) & x < c_0 t \\ f(x - c_0 t) & x > c_0 t \end{cases}$

\medskip
\textbf{12.6.4.}\quad (a) $q = u_{\max}\rho\left(1 - \dfrac{\rho}{\rho_{\max}}\right)$.

\quad (b) $\rho = \rho_{\max}/2$, $u = \dfrac{u_{\max}}{2}$, $q = \dfrac{\rho_{\max}u_{\max}}{4}$

\medskip
\textbf{12.6.8.}\quad (a) $\rho(x,t) = \dfrac{e^{-3(x-ct)}}{3c}(1-e^{-3ct})+f(x-ct)$

\quad (c) $\rho(x,t) = 5t + f\left(x - \frac{1}{2}t^2\right)$

\quad (e) $\rho(x,t) = e^{-t}f\left(x + \frac{1}{3}t^3\right)$

\quad (g) $\rho(x,t) = \frac{1}{2}t^2 + f(xe^{-t})$

\medskip
\textbf{12.6.9.}\quad (a) $\rho(x,t) = e^{3t}f(x_0)$, where $x = x_0 - \frac{1}{6}(e^{6t}-1)f^2(x_0)$

\quad (c) $\rho(x,t) = e^{-t}f(x_0)$, where $x = x_0 + f(x_0)\int_0^t r^2 e^{-\tau}\,d\tau$

\medskip
\textbf{12.6.11.}\quad $u(x,t) = \begin{cases} e^{3t}f\left(x-t-\frac{t^2}{2}\right) & x > t+\frac{t^2}{2} \\ e^{3(t-\tau)}g(\tau) & \text{where } x = t + \frac{t^2}{2} - \frac{3}{7}\tau - \frac{\tau^2}{2},\  x < t + \frac{t^2}{2} \end{cases}$% UNCLEAR: exact form, page 748

\medskip
\textbf{12.6.14.}\quad (a) $-Vf' + u_{\max}\left(1-\dfrac{2\rho}{\rho_{\max}}\right)f' = vf''$

\quad (c) $V = \frac{|q|}{|\rho|} = u_{\max}\left(1-\dfrac{\rho_1+\rho_2}{\rho_{\max}}\right)$

\medskip
\textbf{12.6.17.}\quad (a) $\rho(x,t) = \begin{cases} \rho_{\max} & x < u_{\max}t/5 \\ \dfrac{3\rho_{\max}}{5} & x > u_{\max}t/5 \end{cases}$

\quad (b) $\rho(x,t) = \begin{cases} \rho_1 & x < u_{\max}\left(1-3\dfrac{\rho_1^2}{\rho_{\max}^2}\right)t \\ \dfrac{\rho_{\max}}{\sqrt{3}}\sqrt{1-\dfrac{x}{u_{\max}t}} & \text{otherwise} \\ \rho_2 & x > u_{\max}\left(1-3\dfrac{\rho_2^2}{\rho_{\max}^2}\right)t \end{cases}$% UNCLEAR: exact rarefaction form, page 748

\medskip
\textbf{12.6.18.}\quad (b) $\rho(x,t) = \begin{cases} \rho_{\max} & x < u_{\max}t/5 \\ \dfrac{3\rho_{\max}}{5} & x > u_{\max}t/5 \end{cases}$% UNCLEAR: duplicate of 12.6.17?, page 748

%% ══════════════════════════════════════════════════════════════
\section*{Chapter 13}
%% ══════════════════════════════════════════════════════════════

\textbf{13.2.4.}\quad $\mathcal{L}\left[\int_0^t f(\bar{t})\,d\bar{t}\right] = \dfrac{F(s)}{s}$.

\medskip
\textbf{13.2.5.}\quad (b) $\dfrac{-8s}{(s^2+16)^2}$

\quad (d) $\dfrac{-4}{s^2-6s+25}$

\quad (f) $e^{-5s}\left(\dfrac{2}{s^3}+\dfrac{10}{s^2}+\dfrac{25}{s}\right)-e^{-8s}\left(\dfrac{2}{s^3}+\dfrac{16}{s^2}+\dfrac{64}{s^4}\right)$% UNCLEAR: last term, page 748

\quad (h) $\dfrac{24e^{-s}}{s^5}$

\medskip
\textbf{13.2.6.}\quad (e) $\frac{7}{6}e^{-7t}-\frac{1}{6}e^{-t}$

\quad (j) $\frac{2}{3}+\frac{1}{3}\sin 3t - \frac{2}{3}\cos 3t - 5H(t-4)\left[\frac{2}{3}+\frac{1}{3}\sin 3(t-4)-\frac{2}{3}\cos 3(t-4)\right]$

\medskip
\textbf{13.2.7.}\quad (b) $y = 1 + e^{-t}$

\quad (d) $y = \begin{cases} \frac{25}{7}e^t - \frac{4}{5}e^{-6t} & 0 < t < 3 \\[4pt] -\frac{1}{10}e^{-t}+e^t\left(\frac{25}{7}+\frac{1}{14}e^{-6}\right)+e^{-6t}\left(\frac{1}{35}e^{15}-\frac{4}{7}\right) & t > 3 \end{cases}$% UNCLEAR: exact coefficients, page 748

\quad (f) $y = \frac{1}{3}\sin t - \frac{1}{6}\sin 2t$

\medskip
\textbf{13.3.2.}\quad $G(t,t_0) = \sin(t-t_0)$

\medskip
\textbf{13.4.3.}\quad $u(x,t) = \sin x\cos ct$

\medskip
\textbf{13.4.4.}\quad $\bar{U}(x,s) = F(s)e^{-\sqrt{s/k}\,x}$

\medskip
\textbf{13.5.3.}\quad $\bar{U}(x,s) = \dfrac{cB(s)\sinh sx/c}{s\cosh sL/c}$

\medskip
\textbf{13.6.4.}\quad (a) $\bar{G}(x,s;x_0,t_0) = \dfrac{e^{-st_0}}{\sqrt{sk}}\begin{cases} e^{-\sqrt{s/k}\,x_0}\sinh\sqrt{\frac{s}{k}}\,x & x < x_0 \\[6pt] e^{-\sqrt{s/k}\,x}\sinh\sqrt{\frac{s}{k}}\,x_0 & x > x_0 \end{cases}$

\medskip
\textbf{13.7.1.}\quad (b) $f(t) = \frac{1}{3}\sin 3t$

%% ══════════════════════════════════════════════════════════════
\section*{Chapter 14}
%% ══════════════════════════════════════════════════════════════

\textbf{13.8.1.}\quad $u(x,t) = \sum_{n=1}^{\infty}a_n\sin\dfrac{n\pi x}{L}\cos\dfrac{n\pi ct}{L}$, where $a_n = \dfrac{2}{L}\int_0^L f(x)\sin\dfrac{n\pi x}{L}\,dx$

\medskip
\textbf{14.2.1.}\quad (c) $\omega = -k^2$

\medskip
\textbf{14.2.5.}\quad $\omega^2 = gk\tanh kh$

\medskip
\textbf{14.2.7.}\quad $\omega = g|k|$

\medskip
\textbf{14.3.9.}\quad (a) Let $k = \omega_f^{1/3}$ and $s = \operatorname{sgn}(\omega_f)$. $G(x,0) = \frac{1}{3}(is\sqrt{3}+1)e^{-ikx/2+\sqrt{3}|k|x/2}$,
for $x < 0$. $G(x,0) = \frac{2}{3}e^{ikx}+\frac{1}{3}(is\sqrt{3}-1)e^{-ikx/2-\sqrt{3}|k|x/2}$, for $x > 0$.

\medskip
\textbf{14.5.3.}\quad (b) two waves if $x > -\frac{1}{4}t$ and zero waves if $x < -\frac{1}{4}t$

\medskip
\textbf{14.6.1.}\quad $k = k_0$ and $\omega = \omega(k_0)$

\medskip
\textbf{14.7.8.}\quad $2B = Cu + 2B_0$, $8A = -Cu_{xx}+3Cu^2+4B_0 u + 8A_0$,
$2u_t = -A_{xxx}+4uA_x+2Au_x$

\medskip
\textbf{14.8.6.}\quad (b) $\sigma = -1 + Rk^2 - k^4$, $\omega = k^3$

\quad (c) $\sigma = k$, $\omega = 0$ ill-posed since $\sigma \to \infty$ as $k \to +\infty$

\medskip
\textbf{14.9.3.}\quad $\psi ={}$constant, and $2\omega\od{r}{T}+\od{\omega}{2T}r+3\omega^3 r^3 = 0$

\medskip
\textbf{14.9.5.}\quad $u = cL^{-3/4}\cos\!\left(\int\sqrt{\dfrac{L(\xi)}{T(\xi)}}\,dt + \psi\right)$% UNCLEAR: exact form, page 749

\medskip
\textbf{14.9.8.}\quad $k = \theta_x$, $\omega = -\theta_t$, $k_T + 3k^2 k_X = 0$, $u_0 = Ae^{i\theta}+(*)$, $A_T + 3k^2 A_X + 3kk_X A = 0$

\medskip
\textbf{14.10.3.}\quad Outer $u_0 = -\frac{4}{x}$. Inner (right) $U_0 = -\frac{1}{4}+\frac{9}{4}e^{2X}$, where $x - 1 = \varepsilon^{1/2}X$.
Inner (left) $U_0 = e^{-2X}$, where $x = \varepsilon^{1/2}X$.

\medskip
\textbf{14.10.6.}\quad Outer $u_0 = 6(2x+1)^{-1}$. Inner $U_0 = 6-5e^{-X}$, where $x = \varepsilon X$.
