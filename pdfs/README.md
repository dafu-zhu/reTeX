# Source PDFs

Place your scanned textbook PDF here as `scanned.pdf`.

```bash
cp /path/to/your/textbook.pdf pdfs/scanned.pdf
```

Then run `/pdf-to-latex pdfs/scanned.pdf`.

PDFs are gitignored — they are not committed to the repo.
